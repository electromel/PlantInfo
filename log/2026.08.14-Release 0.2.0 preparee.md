# 2026.08.14 — Release 0.2.0 préparée (bundle signé)

## Demande

Passer le seuil d'alerte toxique de 70 à 80, builder, préparer une version pour publication, et
guider la suite côté Play Console.

## Fait

### 1. Seuil porté à 80

`IdentificationResult.TOXIC_ALERT_MAX_SCORE` : 70 → **80**. Test de bornes ajusté (79 déclenche,
80 non). Voir `log/2026.08.14-Alerte confusion toxique.md`.

### 2. Version

`versionCode = 2` / `versionName = "0.2.0"`, déjà en place depuis le lot du 16.07 et **jamais
publiés** : seul le couple 1 / 0.1.0 est sorti (test interne du 13.07). 0.2.0 couvre donc l'ensemble
du travail depuis : enrichissement Pl@ntNet (GBIF, UICN, noms français), dimensions à maturité,
calendrier d'entretien, usages, symbolique, alerte de confusion toxique, PDF paginé.

Le correctif de `HistoryRepository.selectSpecies()` (effacement des champs d'espèce à la validation
manuelle, `FungusChecker`) est arrivé dans l'arbre de travail pendant la préparation : le bundle a
été **reconstruit** ensuite pour que l'artefact corresponde au code livré.

### 3. Artefacts produits

```
app/build/outputs/bundle/release/app-release.aab   15,3 Mo   ← à téléverser sur la Console
app/build/outputs/apk/release/app-release.apk      52,5 Mo   ← test local uniquement (APK universel)
```

`lintVitalRelease` passe (c'est le lint bloquant de release, exécuté automatiquement par
`bundleRelease`).

### 4. Contrôles avant publication

- **Signature** : `jarsigner -verify` → `jar verified`, `CN=Reynard Michel`, certificat valide
  jusqu'au 2053-11-27. **Même clé d'upload que 0.1.0** — c'est la condition pour que la Console
  accepte la mise à jour.
- **Aucune clé API dans le build publié** : `BuildConfig.java` du variant `release` vérifié, les
  quatre `DEFAULT_*_API_KEY` valent `""`. Les clés de `dev-keys.properties` ne sont injectées qu'en
  `debug`.
- **Tests** : 42 tests, 0 échec (dont les 11 de `HistoryRepositoryTest`, arrivés avec le correctif).

### 5. Textes de la fiche Store (`docs/play-store-listing.md`)

- Section « Notes de version — 0.2.0 » ajoutée, **452 caractères** (limite Console : 500).
- Notes 0.1.0 conservées et datées, pour garder l'historique des releases.
- Bloc FONCTIONNALITÉS de la description complète enrichi des nouveautés (calendrier, usages,
  symbolique, taille à maturité, UICN). Description complète : 1905 / 4000 caractères ; description
  courte : 75 / 80.

## Reste à faire (côté utilisateur, dans la Play Console)

Non automatisable ici : téléversement du `.aab`, notes de version, déploiement sur la piste de test
interne. Le point bloquant pour la production reste celui du 13.07 : compte développeur personnel
récent → **test fermé avec ≥ 12 testeurs pendant 14 jours** exigé par Google.

## À surveiller pendant ce test

1. Le seuil à 80 rend l'avertissement toxique nettement plus fréquent qu'à 70 : vérifier qu'il ne
   devient pas un bandeau rouge permanent auquel on n'ait plus l'œil.
2. La migration Room **v5 → v6** s'exécute pour la première fois sur un appareil réel : une base
   créée en 0.1.0 (v4 ou v5) doit s'ouvrir sans crash au démarrage.
3. Le PDF paginé n'a pas encore été ouvert sur un appareil : contrôler les sauts de page et le pied
   de page numéroté.
